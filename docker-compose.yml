services:
  app:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - db-data:/app/server
      - uploads:/app/server/uploads
    restart: unless-stopped

volumes:
  db-data:
  uploads:
